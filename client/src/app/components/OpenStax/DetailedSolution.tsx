import CollaboratorSolution from './CollaboratorSolution';
import { SingleInputProps } from './types';

export default function DetailedSolution(props: SingleInputProps) {
  return <CollaboratorSolution title={'Detailed Solution'} {...props} />;
}
