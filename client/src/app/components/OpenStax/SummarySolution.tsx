import CollaboratorSolution from './CollaboratorSolution';
import { SingleInputProps } from './types';

export default function SummarySolution(props: SingleInputProps) {
  return <CollaboratorSolution title={'Summary Solution'} {...props} />;
}
